import SwiftUI
import UniformTypeIdentifiers

struct ContentView: View {
    var body: some View {
        AdaptiveContentView()
    }
}

#Preview {
    ContentView()
        .frame(width: 1000, height: 700)
}
